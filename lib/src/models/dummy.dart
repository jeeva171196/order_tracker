class DummyDetail {
  String? id;
  String? name;
  int? numOfBundles;
  int? numOfSteps;

  Map<int, String>? steps;

  DummyDetail(
      {this.id, this.name, this.numOfBundles, this.numOfSteps, this.steps});
}
